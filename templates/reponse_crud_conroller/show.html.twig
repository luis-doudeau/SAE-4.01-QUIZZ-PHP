{% extends 'base.html.twig' %}

{% block title %}Reponse{% endblock %}

{% block body %}
    <h1>Reponse</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reponse.id }}</td>
            </tr>
            <tr>
                <th>NomReponse</th>
                <td>{{ reponse.nomReponse }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_reponse_crud_conroller_index') }}">back to list</a>

    <a href="{{ path('app_reponse_crud_conroller_edit', {'id': reponse.id}) }}">edit</a>

    {{ include('reponse_crud_conroller/_delete_form.html.twig') }}
{% endblock %}

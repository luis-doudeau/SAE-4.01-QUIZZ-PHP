{% extends 'base.html.twig' %}

{% block title %}Reponse{% endblock %}

{% block body %}
    <div class="row justify-content-center">
        <div class="col-md-8">
            <h1 class="mb-4">Reponse</h1>
            <table class="table table-bordered">
                <tbody>
                    <tr>
                        <th>Id</th>
                        <td>{{ reponse.id }}</td>
                    </tr>
                    <tr>
                        <th>NomReponse</th>
                        <td>{{ reponse.nomReponse }}</td>
                    </tr>
                </tbody>
            </table>

            <div class="mt-4">
                <a class="btn btn-secondary mr-2" href="{{ path('app_reponse_crud_controller_index') }}">Retour à la liste</a>
                <a class="btn btn-primary" href="{{ path('app_reponse_crud_controller_edit', {'id': reponse.id}) }}">Modifier</a>
            </div>

            <div class="mt-4">
                {{ include('reponse_crud_controller/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}

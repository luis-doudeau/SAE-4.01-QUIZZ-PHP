{% extends 'base.html.twig' %}

{% block title %}New Question{% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-md-8 mx-auto">
                <h1 class="text-center">Create new Question</h1>

                <div class="card">
                    <div class="card-body">
                        {{ include('question_crud/_form.html.twig') }}

                        <div class="text-center mt-3">
                            <a href="{{ path('app_question_crud_index') }}" class="btn btn-secondary mr-2">Back to list</a>
                            <button type="submit" form="question-form" class="btn btn-primary">Create</button>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}

{% extends 'base.html.twig' %}

{% block title %}Question{% endblock %}

{% block body %}
    <h1>Question</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ question.id }}</td>
            </tr>
            <tr>
                <th>NomQuestion</th>
                <td>{{ question.nomQuestion }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_question_crud_index') }}">back to list</a>

    <a href="{{ path('app_question_crud_edit', {'id': question.id}) }}">edit</a>

    {{ include('question_crud/_delete_form.html.twig') }}
{% endblock %}
